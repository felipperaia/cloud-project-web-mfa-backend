# MongoDB Atlas
MONGODB_URI="mongodb+srv://<user>:<pass>@cluster0.xxxxxx.mongodb.net/dbname?retryWrites=true&w=majority"

# App settings
APP_SECRET_KEY="uma-string-secreta-grande"
JWT_SECRET="jwt-secret-string"
JWT_ALGORITHM="HS256"

# Email (SendGrid / SMTP)
SMTP_HOST="smtp.sendgrid.net"
SMTP_PORT=587
SMTP_USER="apikey"
SMTP_PASSWORD="SG.xxxxxxx"

# App runtime
PORT=8000
FRONTEND_URL="http://localhost:3000"
RENDER=true
